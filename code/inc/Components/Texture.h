#pragma once

class Texture
{
    public:
        Texture();
        ~Texture();
    private:
        //SDL_Texture* mTexture;
        //SDL_Rect mRect;
        //SDL_Renderer* mRenderer;
        //std::string mPath;
};