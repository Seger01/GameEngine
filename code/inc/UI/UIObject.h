#pragma once
#include "Button.h"
#include "Text.h"

class UIObject
{
    public:
        UIObject();
        ~UIObject();
        //add UIObject functions and remove UIObject functions?
    private:
        int mWidth;
        int mHeight;
};